on:
  push:
    branches:
      - main

name: 🚀 Deploy website on push

jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v4

      - name: 📂 Sync files
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server:   ${{ secrets.FTP_HOST }}       # ex: ftp.example.com
          username: ${{ secrets.FTP_USERNAME }}   # ex: deploy@example.com
          password: ${{ secrets.FTP_PASSWORD }}
          protocol: ftps                          # ou 'ftp' selon ton hébergeur
          port: 21                                # ajuste si besoin
          local-dir: ./                           # dossier local à envoyer
          server-dir: /wp-content/themes/mon-theme/ # dossier cible sur le serveur

